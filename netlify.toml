[build]
  command = "npm run build"
  publish = "dist"
  environment = { NODE_ENV = "production", NPM_FLAGS = "--include=dev" }

[context.production.environment]
  # Defina `VITE_GEMINI_API_KEY` apenas no painel do Netlify (Site settings → Environment variables).
  # Não configure valores sensíveis aqui para evitar sobreposição com a UI e exposição no repositório.
